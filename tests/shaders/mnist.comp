#version 460

layout (binding = 0) readonly buffer SrcBuffer {
    int x[];
} src;

layout (binding = 1) buffer DstBuffer {
    int x[];
} dst;

void main() {
    uint gID = gl_GlobalInvocationID.x;
    if (gID < 256) {
        dst.x[gID] = src.x[gID] * 2;
    }
}